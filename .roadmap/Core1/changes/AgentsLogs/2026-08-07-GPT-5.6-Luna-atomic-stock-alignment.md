# Core 1 — Atomic stock alignment log

## Date: 2026-08-07
model: GPT-5.6 Luna
Route: `.policies/warehouse/WAREHOUSE_POLICY.md`
Description: Actualicé la política Warehouse para reflejar la decisión aprobada para Core 1: la lógica de reserva permanece en los clientes Android/Web, Appwrite continúa siendo la fuente de verdad, las mutaciones de `reserved` utilizan operaciones numéricas atómicas, la cache local no es autoridad para mutaciones críticas, Pusher actúa únicamente como señalizador y la garantía transaccional completa mediante Function/Transaction queda explícitamente reservada para Core 2. También documenté la compensación de soft-holds multi-producto y las limitaciones de concurrencia que siguen existiendo entre `existence` y `reserved` fuera de una transacción de negocio.

## Date: 2026-08-07
model: GPT-5.6 Luna
Route: `app/src/main/java/com/elitec/alejotaller/feature/product/domain/repository/ProductNetRepository.kt`
Description: Reemplacé el contrato de escritura directa de `reserved` por operaciones específicas `incrementReserved` y `decrementReserved`, preparando la capa de red Android para delegar la mutación al operador atómico de Appwrite. El incremento recibe un máximo basado en `existence` y el decremento utiliza mínimo `0`.

## Date: 2026-08-07
model: GPT-5.6 Luna
Route: `app/src/main/java/com/elitec/alejotaller/feature/product/data/repository/ProductNetRepositoryImpl.kt`
Description: Implementé `Databases.incrementDocumentAttribute` y `Databases.decrementDocumentAttribute` del SDK de Appwrite. Se eliminó el patrón remoto `GET → calcular reserved → updateDocument` para estas mutaciones críticas. El incremento utiliza `max=existence` y el decremento `min=0`, aprovechando las primitivas atómicas documentadas por Appwrite.

## Date: 2026-08-07
model: GPT-5.6 Luna
Route: `app/src/main/java/com/elitec/alejotaller/feature/product/data/repository/ProductOfflineFirstRepository.kt`
Description: Cambié la implementación offline-first Android para que las mutaciones críticas de stock sean remote-first: obtiene `existence` desde Appwrite para determinar el máximo de la operación atómica, ejecuta el incremento/decremento remoto y solo después persiste en Room la respuesta autoritativa. Se eliminó el fallback a Room para decidir una mutación de stock.

## Date: 2026-08-07
model: GPT-5.6 Luna
Route: `app/src/main/java/com/elitec/alejotaller/feature/product/domain/caseUse/ApplySoftHoldCaseUse.kt`
Description: Mantuvimos la lógica de negocio de soft-hold en Android como exige Core 1, pero ahora la escritura de `reserved` pasa por la operación atómica del repositorio. La validación crítica exige `refreshFromRemote` desde Appwrite. Para ventas multi-producto se registran las cantidades realmente confirmadas y, si una línea posterior falla, se ejecuta compensación mediante decrementos atómicos. También se preserva `CancellationException` para no convertir cancelaciones de coroutine en operaciones de compensación artificiales.

## Date: 2026-08-07
model: GPT-5.6 Luna
Route: `app/src/test/java/com/elitec/alejotaller/data/fakesRepositories/product/FakeProductNetRepository.kt`
Description: Actualicé el fake de red JVM para implementar el nuevo contrato de operaciones atómicas y simular límites de `maxReserved` y `min=0`.

## Date: 2026-08-07
model: GPT-5.6 Luna
Route: `app/src/androidTest/java/com/elitec/alejotaller/fakes/FakeProductNetRepository.kt`
Description: Alineé el fake de instrumentación Android con el nuevo contrato `incrementReserved/decrementReserved`, permitiendo que las pruebas que usan la infraestructura de producto sigan representando la semántica de stock atómico.

## Date: 2026-08-07
model: GPT-5.6 Luna
Route: `app/src/test/java/com/elitec/alejotaller/data/fakesRepositories/product/FakeProductRepository.kt`
Description: Amplié el fake de dominio para registrar incrementos y decrementos, permitir simulación de fallos de incremento por producto y exponer `refreshFromRemote`. Esto permite probar la compensación de soft-holds parciales sin introducir dependencias reales de Appwrite.

## Date: 2026-08-07
model: GPT-5.6 Luna
Route: `app/src/test/java/com/elitec/alejotaller/feature/product/domain/caseuse/SoftHoldCaseUseTest.kt`
Description: Extendí las pruebas Android del soft-hold para validar la ruta de mutación atómica y el escenario multi-producto donde una línea posterior falla y las reservas previamente confirmadas deben ser compensadas.

## Date: 2026-08-07
model: GPT-5.6 Luna
Route: `app/src/test/java/com/elitec/alejotaller/feature/product/domain/caseuse/ReleaseSoftHoldCaseUseTest.kt`
Description: Creé pruebas unitarias Android para el rollback de soft-hold, verificando delegación a `decrementReserved` y el comportamiento de clamp a cero.

## Date: 2026-08-07
model: GPT-5.6 Luna
Route: `web/src/core/feature/product/domain/repository/product.repository.ts`
Description: Añadí al contrato Web las operaciones de mutación atómica `incrementReserved` y `decrementReserved`, además de un `refreshFromRemote` opcional para que los casos de uso críticos puedan exigir una lectura autoritativa sin romper mocks/fakes existentes.

## Date: 2026-08-07
model: GPT-5.6 Luna
Route: `web/src/core/feature/product/data/repository/product.net.repository.ts`
Description: Implementé las operaciones atómicas de Appwrite para Web mediante `incrementDocumentAttribute` y `decrementDocumentAttribute`. El incremento utiliza `existence` como máximo y el decremento utiliza `0` como mínimo.

## Date: 2026-08-07
model: GPT-5.6 Luna
Route: `web/src/core/feature/product/data/repository/product.offline-first.repository.ts`
Description: Añadí `refreshFromRemote` y las mutaciones atómicas de stock. La respuesta remota autoritativa se persiste en Dexie; un fallo de la mutación no hace fallback a Dexie para modificar stock. La lectura remota previa del incremento determina el límite `existence` utilizado por Appwrite.

## Date: 2026-08-07
model: GPT-5.6 Luna
Route: `web/src/core/feature/sale/domain/caseuse/RegisterNewSaleCaseUse.ts`
Description: Mantuvimos la lógica de reserva dentro del cliente Web para Core 1, pero reemplazamos `update({reserved: nextReserved})` por `incrementReserved`. La validación crítica intenta `refreshFromRemote`. Se conserva la cola local por `productId` y se añadió compensación atómica de las líneas confirmadas cuando falla una línea posterior, serializando también esas compensaciones por producto.

## Date: 2026-08-07
model: GPT-5.6 Luna
Route: `web/src/core/feature/product/domain/caseuse/ReleaseSoftHoldCaseUse.ts`
Description: Reemplacé el patrón Web `getById → calcular reserved → update` por `decrementReserved`, de modo que el rollback sea atómico y server-authoritative, con la respuesta actualizada persistida por el repositorio.

## Date: 2026-08-07
model: GPT-5.6 Luna
Route: `web/src/test/core/feature/sale/domain/caseuse/RegisterNewSaleCaseUse.soft-hold.test.ts`
Description: Actualicé las pruebas Web para validar la delegación a la operación atómica, la lectura remota, el rechazo de la mutación y la compensación de reservas previamente confirmadas en ventas multi-producto.

## Date: 2026-08-07
model: GPT-5.6 Luna
Route: `web/src/test/core/feature/product/domain/caseuse/ReleaseSoftHoldCaseUse.atomic.test.ts`
Description: Creé pruebas unitarias Web para asegurar que el rollback usa `decrementReserved` y no vuelve a introducir el patrón read-modify-write.

## Date: 2026-08-07
model: GPT-5.6 Luna
Route: `web/src/test/core/feature/product/data/repository/product.offline-first.repository.atomic.test.ts`
Description: Creé pruebas del repositorio offline-first Web para comprobar que `existence` remoto se utiliza como máximo, que la respuesta de Appwrite se almacena en Dexie y que un fallo remoto no cae en una mutación local.

## Date: 2026-08-07
model: GPT-5.6 Luna
Route: `.roadmap/Core 1/changes/AgentsLogs/2026-08-07-GPT-5.6-Luna-atomic-stock-alignment.md`
Description: Creé este registro para dejar trazabilidad reproducible de las modificaciones realizadas en ambas plataformas, la política actualizada, las decisiones de concurrencia y las pruebas añadidas. El objetivo es que futuros agentes puedan distinguir claramente Core 1 de la futura extracción transaccional a Function en Core 2.

## Validación realizada antes del Pull Request

- Comparación `core-1` contra `master`: rama adelantada sin divergencia respecto a `master` al momento de la revisión.
- Se revisaron los archivos modificados y sus contratos para comprobar que Android y Web mantienen la misma semántica: validación remota → mutación atómica de `reserved` → persistencia local de la respuesta → señalización Pusher.
- Se verificó contra la documentación oficial actual de Appwrite que `incrementDocumentAttribute`/`decrementDocumentAttribute` soportan `max`/`min` y son las primitivas apropiadas para eliminar el patrón read-modify-write en el SDK de Documents usado por el proyecto.
- La ejecución definitiva de las suites queda delegada al CI del Pull Request; no se marca manualmente como pasada ninguna prueba que no haya sido ejecutada por el runner.

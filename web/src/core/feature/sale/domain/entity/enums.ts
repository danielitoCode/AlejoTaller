export enum BuyState {
    UNVERIFIED = "UNVERIFIED",
    VERIFIED = "VERIFIED",
    DELETED = "DELETED"
}

export enum DeliveryType {
    PICKUP = "PICKUP",
    DELIVERY = "DELIVERY"
}

export enum Currency {
    CUP = "CUP",
    USD = "USD",
    MLC = "MLC"
}
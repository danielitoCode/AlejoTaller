# One-shot helper desactivado (el if sin comillas rompía el parseo YAML en Actions).
name: Fix reservation icon (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Workflow desactivado; se puede eliminar este archivo."
